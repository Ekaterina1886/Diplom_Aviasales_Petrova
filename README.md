# Финальная работа по ручному тестированию Aviasales (UI and API Tests) (Selenium + Pytest + Page Object + Allure)

Проект содержит 5 UI и 5 API автотестов для сайта https://www.aviasales.ru/.
Тесты выполнены в архитектуре Page Object, включают Allure шаги, ожидания,
фикстуры WebDriver и параметризацию.

## 📁 Структура проекта

- tests/ — тест-кейсы
- pages/ — Page Object уровни
- conftest.py — фикстура для WebDriver
- requirements.txt — зависимости

## 🚀 Запуск тестов

Установить зависимости:

pip install -r requirements.txt


Запуск всех тестов:

pytest -s -v

Командa 
pytest --alluredir=./allure_result_all

После завершения тестирования вводим команду:
allure serve allure_result_all    для просмотра отчета о тестировании


### API тесты (5 тестов)

Используются запросы к https://tickets-api.aviasales.ru/search/v2/start.

Тесты проверяют:

1. Успешный старт поиска билетов (POST)
2. Ошибочный запрос с некорректными параметрами
3. Проверку обязательных полей
4. Корректность структуры ответа
5. Мультисегментный поиск

API тесты включают:
Параметризацию дат и направлений
Allure шаги
Фикстуру API клиента

#### Запуск API тестов:

Команда pytest tests/test_api.py --alluredir=./allure_result_api
После завершения тестирования вводим команду allure serve allure_result_api для просмотра отчета о тестировании


## 🧪 Набор UI тестов

1. Поиск авиабилетов  
2. Календарь низких цен  
3. Фильтр «Только прямые рейсы»  
4. Смена языка сайта  
5. Обратный поиск (swap)

#### Запуск UI тестов:

Команда pytest tests/test_ui.py --alluredir=./allure_result_ui
После завершения тестирования вводим команду allure serve allure_result_ui для просмотра отчета о тестировании



## Стек технологий

pytest - основная библиотека для написания и выполнения тестов.
selenium - библиотека для автоматизации UI тестирования.
requests - библиотека для работы с HTTP-клиентом, используемая для API тестирования.
allure - библиотека для генерации отчетов о выполнении тестов.

README.md - отчет-инструкция к работе